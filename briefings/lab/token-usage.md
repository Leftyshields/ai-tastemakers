# Narration cost & quality · Lab

This page is generated at build time from `data/quality/token-usage.jsonl` and `data/quality/rubric-scores.jsonl`.

Open [token-usage.html](./token-usage.html) after `npm run build:pages` for the reader-friendly dashboard. Field definitions and operator commands live in the **Technical reference** section at the bottom of that page.
